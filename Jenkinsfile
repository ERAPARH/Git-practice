pipeline {
    agent any

    stages {
        stage('Run Python Script') {
            steps {
                echo 'Running test.py...'
                sh 'python3 test.py'
            }
        }
    }
}
