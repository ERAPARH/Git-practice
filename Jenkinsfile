pipeline {
    agent any

    stages {
        stage('Run Python Script') {
            steps {
                echo 'Running hello.py...'
                sh 'python3 hello.py'
            }
        }
    }
}
